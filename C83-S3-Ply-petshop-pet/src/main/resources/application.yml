server:
   port: 8001
   
# 定义当前服务名
spring:
   application:
      name: psbook
   datasource:
      url: jdbc:mysql://127.0.0.1/c83-s3-ply-pets?serverTimezone=UTC&CharacterEncoding=UTF-8&useSSL=false
      username: root
      password: a
      driver-class-name: com.mysql.jdbc.Driver

logging:
   level:
      com.yc.crbook.dao: debug